# internal

The following files are copied from go's src and modified because calling `internal/testenv`
is prohibited.

I am only including `internal/testenv` because this module is a fork/fix of go's original
`flag` functions and their original source uses `internal/testenv`.